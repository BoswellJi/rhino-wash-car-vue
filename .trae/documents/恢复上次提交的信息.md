## 恢复计划

### 问题分析
从git状态和差异来看，当前只有`types/auto/components.d.ts`文件被修改，其中很多Element Plus组件的类型声明被删除了，包括：
- ElBadge
- ElCard
- ElConfigProvider
- ElDialog
- ElEmpty
- ElForm
- ElFormItem
- ElImage
- ElInput
- ElPopover
- ElTabPane
- ElTabs
- ElTag

### 解决方案
1. **恢复被删除的组件类型声明**：使用git命令恢复`types/auto/components.d.ts`文件到上次提交的状态
2. **验证恢复结果**：检查文件内容确保所有被删除的组件类型声明都已恢复

### 实施步骤
1. 运行`git checkout HEAD -- types/auto/components.d.ts`命令，将文件恢复到上次提交的状态
2. 运行`git status`检查文件状态，确认修改已恢复
3. 运行`git diff`确认没有未提交的更改

### 预期结果
- `types/auto/components.d.ts`文件恢复到上次提交的状态
- 所有被删除的Element Plus组件类型声明都已恢复
- git工作区干净，没有未提交的更改